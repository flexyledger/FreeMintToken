// Import dependencies
import React from 'react';

// Define the component
const MyComponent: React.FC = () => {
  return (
    <div>
      <h1>Hello, World!</h1>
      <p>This is a template for a front-end using Next.js with TypeScript.</p>
    </div>
  );
};

// Export the component
export default MyComponent;
